(function() {
    webapp.controller('LotPopupController', [
        '$scope',
        '$log',
        function($scope, $log) {
            $log.debug('lotPopupController loaded');
        }
    ]);
})();

(function() {
    webapp.controller('ConsignmentController', [
        '$scope',
        '$log',
        function($scope, $log) {
            $log.log('ConsignmentController loaded');
        }
    ]);
})();
